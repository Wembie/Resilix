# Proto

This directory is reserved for future API contracts such as:

- control-plane RPCs
- telemetry envelopes
- event schemas for worker orchestration

The Redis SDK itself does not require protobuf today, but the repository is intentionally prepared for an event-driven and service-oriented future.
